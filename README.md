# วงไพ่สายดื่ม

โฟลเดอร์นี้ถูกแยกเป็น 2 โปรเจคย่อย:

- `web-app` สำหรับเว็บเกมหลัก
- `mobile-app` สำหรับแอปมือถือ Android/iOS ที่แพ็กจากเว็บเกม

คำสั่งใช้งานหลัก:

```bash
cd web-app
npm run dev
```

```bash
cd mobile-app
npm run cap:sync
```
